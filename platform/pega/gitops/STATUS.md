# GitOps validation status

Release and promotion contracts are present. Argo CD synchronization against a Pega runtime is not validated yet. Generic GitOps mechanics can be tested independently on CRC.